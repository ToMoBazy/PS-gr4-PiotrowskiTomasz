#include <stdio.h>
#include <stdlib.h>

int main()
{int liczba;
    scanf("%d", &liczba);
    if(liczba<0)
    {
        printf("%d", liczba=liczba*-1);
    }
    else{

        printf("%d", liczba);

    }

    return 0;
}
